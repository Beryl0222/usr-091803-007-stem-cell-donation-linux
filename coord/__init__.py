"""造血干细胞捐献协同后端（中华骨髓库跨机构协同）。

领域包：
- clock       统一 UTC 存储与各中心本地时间解释
- identity    机构、人员、角色目录
- models      领域对象（检索需求、捐献病例、同意版本、时间槽、采集物、交接、温控异常、通知）
- audit       监管可复核的哈希链审计记录
- store       带锁的内存存储与幂等索引
- routing     “只通知真正需要处理的人”的受众策略
- workflow    事件驱动的状态机（幂等、版本化、不可改写同意）
- projection  按职责最小化的字段脱敏
- timeline    面向任一采集物的完整时间线
- api         JSON HTTP 接口
"""

__version__ = "1.0.0"
